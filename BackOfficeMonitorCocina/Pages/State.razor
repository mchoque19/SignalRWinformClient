@inject DAL.Interfaces.IGenericRepository<DAL.DAO.State> _StateRespository
@using Microsoft.AspNetCore.Components.Authorization;
@attribute [Authorize]

@page "/state"
<h3>State</h3>
<div class="row">
    <div class="col-12 text-aling-right">
        <a href="stateNew">Nuevo</a>
    </div>
</div>
@if (Data.Any())
{
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th></th>
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Data)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <td> <a href="/stateNew/@item.Id">Editar</a> </td>
                    @* <td><a href="/eliminar/@item.Id">Eliminar</a></td> *@
                </tr>
            }

        </tbody>
    </table>
}
@code {
    private IEnumerable<DAL.DAO.State> Data = Array.Empty<DAL.DAO.State>();

    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
        Data = await _StateRespository.GetAll();
    }
}
